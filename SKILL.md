---
name: travel-roadbook
description: >
  把一段自驾行程文本变成路书：逐日驾车路线图、段间里程与耗时、景点实景照片、
  海拔与天气，产出本地可交互页面 + 可分享的单文件 HTML / PDF / 图片。
  数据源：高德（地理编码/驾车路径/POI 照片）、Open-Meteo（天气与海拔，免 key）、
  小红书（游客实拍图与原帖链接，可选）。
  触发场景：用户贴出「1:成都—折多山——住新都桥 2:……」这类分天行程文本并希望
  「生成路线和图片」「做个路书」「标出每段距离和时间」「加上天气」「转成能分享的」；
  或说「更新路书数据」「重新生成分享页」「把路书部署到服务器」。
---

# travel-roadbook

行程文本 → 结构化数据 → 路线图 + 照片 + 天气 → 可分享路书。

**工具位置**：`~/.claude/skills/travel-roadbook/tools/`
**许可**：见 `NOTICE.md` —— 含 GPL-2.0 组件（小红书签名引擎），删掉即可宽松分发
**示例**：`examples/itinerary.chuanxi.txt`（川西 6 天）、`examples/aliases.chuanxi.json`

这是个**解析器**不是规划器 —— 用户的路线已经定死，任务是把它变准确、可视、可分享，
不是替用户重新规划行程。

## 何时用

用户已有一份分天行程文本，想要地图、照片、里程耗时、天气。若用户只说「帮我规划去
XX 玩几天」而没有具体点位，这个 skill 不适用。

## 准备

在用户的工作目录（不是 skill 目录）建项目，把 tools 拷过去：

```bash
mkdir -p <项目目录> && cd <项目目录>
cp -r ~/.claude/skills/travel-roadbook/tools/* .
# index.html 顶部的 AMAP_* 是占位符, 首次使用请填自己的高德 key
python3 -m venv .venv && .venv/bin/pip install -r requirements.txt   # 仅小红书需要
npm install        # 仅小红书需要(crypto-js + jsdom, 签名引擎依赖)
```

**必须问用户要的**：
- **高德 Web 服务 key**（必需）—— 地理编码 + 驾车路径 + POI 照片全靠它，[lbs.amap.com](https://lbs.amap.com) 免费不绑卡
- **出发日期**（强烈建议）—— 没有日期天气就只能给"今天起 N 天"，对不上行程
- 高德 Web端(JS API) key + 安全密钥（仅交互版地图需要，与上面那个是**两个不同的 key**）
- 小红书 Cookie（仅补图需要，见下方"小红书"一节）

## 流程

### 1. 行程文本 `itinerary.txt`

每行一天，`N:` 开头，点位用 `—` 分隔：

```
1:成都—天全服务区网红打卡—折多山—格底拉姆天空之城——住新都桥
2:住宿地—雅江天路十八弯—勒通古镇——住理塘
3:理塘—格聂南线（铁匠山垭口，下则通村，格聂之眼）——住巴塘
```

解析规则：`住X` = 当日宿地（自动作为当日终点和次日起点）；`住宿地` = 沿用前一晚；
括号内逗号分隔项展开为并列途经点；描述性词（网红打卡/反穿/海拔4900 等）自动剥离。

### 2. 地名修正表 `aliases.json`（**最关键的一步**）

```json
{
  "格底拉姆天空之城": {"query": "格底拉姆·天空之城", "city": "康定"},
  "长庆春科尔寺":     {"query": "长青春科尔寺", "city": "理塘"},
  "格聂南线":         {"query": null, "city": null}
}
```

- `query` 是喂给高德的检索词，`city` 是城市限定，`query: null` 表示这是路线名不是点位
- **每个点位都要给 city**。缺了它高德会跨市模糊匹配，实测把「格底拉姆天空之城」匹配到
  95km 外的同名 POI，D1 里程从 378km 虚增到 644km，**且 API 全程返回成功、不报错**
- 先跑一遍，根据输出的警告和「原文 → 命中名称」对照反复修正这张表，直到警告清零

### 3. 生成

```bash
AMAP_KEY=xxx .venv/bin/python build_route.py itinerary.txt -o data.json \
    --weather --start 2026-08-17 --xhs 2
```

`--weather` 天气+海拔 · `--start` 绑定日期 · `--xhs N` 每点位补 N 张小红书图
`--refresh` 全量重抓 · `--refresh-xhs` 只重抓小红书 · `--aliases` 换地名表

### 4. 核对警告（**别跳过**）

脚本会输出「N 条待核对」，逐条看：
- `跨市模糊匹配` → 该点位的 city 限定没生效，多半是地名写法要改
- `距当日途经点重心 XXkm, 疑似定位错误` → 几乎一定是匹配错了地方
- `直线 Akm 但驾车 Bkm (N倍)` → 该路段高德没有路网（非铺装/越野），**折线不代表实际轨迹**，
  里程和耗时都不可信，要在交付物里明确写出来

### 5. 查看

```bash
python3 -m http.server 8899     # 然后开 localhost:8899/index.html
```

`index.html` 是本地开发/查看工具，**不能直接分享**（`fetch('data.json')` 在 `file://`
下被拦、照片是外链、地图要联网）。

### 6. 分享物

```bash
AMAP_KEY=xxx .venv/bin/python make_share.py
```

产出：
- `share.html` — 托管版正文（无 `<head>`，给 Artifact 之类平台用）
- `<标题>.html` — 独立单文件，双击即开、零外部请求、可离线
- 要 PDF/图片：`chrome --headless --no-pdf-header-footer --print-to-pdf=out.pdf file://<独立版>`，
  再用 PDFKit 把 PDF 逐页导成 PNG（微信发文件用 PDF、发图用 PNG）

## 数据源与各自的代价

| 数据 | 来源 | 注意 |
|---|---|---|
| 坐标/路线/POI 照片 | 高德 Web 服务 | 一个 key 全包；免费额度并发极低，`10021` 是常态，已内置退避重试 |
| 天气/海拔 | Open-Meteo，免 key | 按坐标而非按区县 —— 高原一个县内海拔差 2000m，按区县的天气没有参考价值 |
| 历史同期 | Open-Meteo Archive (ERA5) | 出发日超出 16 天预报窗口时，这是唯一有参考价值的依据 |
| 游客实拍图 | 小红书（可选） | 限流严重、cookie 会过期、图链有时效签名 |

## 必须知道的坑

这些都是实测踩出来的，别重蹈：

1. **地理编码错了不会报错**，只会安静地给一条绕远的路线。所以必须有离群检测（点位距当日
   重心 >120km 告警）和绕行比（驾车里程/直线距离 >2.5 告警）。
2. **高德首条命中不一定最优**。「毛垭大草原」首条返回「大毛垭坝养护站」。已按
   名称吻合度 > 景点类型 > 有图 打分，并排斥养护站/收费站/村委会。
3. **最准的 POI 常常没有照片**（「勒通古镇」精确匹配那条 0 图，而「勒通古镇·千户藏寨旅游景区」
   有 3 张）。策略：保留准坐标，从 8km 内同名候选借图。
4. **高德对空值返回 `[]` 而不是 `""`**，直接当字符串格式化会 `TypeError`。用 `s()` 包一层。
5. **缓存必须增量落盘**。限流随时会中断，只在最后 `flush()` 一次的话前面全白抓。
6. **缓存读取要深拷贝 `photos` 列表**。浅拷贝会让多天共用同一个 list，后续 `+=` 就地扩展
   把缓存那份也撑大（重复出现的点位图片累积到 8 张），而且脏数据会被 flush 进磁盘。
7. **小红书 461 是账号级请求配额**，不是单点位问题。跑到第 20 个左右必中，退避重试无用 ——
   遇到就中止整轮，靠增量缓存下次续上。
8. **CSS 全局 `svg` 选择器会污染高德内部 SVG 图层**，把折线挤出可视区（marker 是 DOM 元素
   所以不受影响，现象是「有点没线」，且无任何报错）。选择器一定要限定 id。
9. **`@media print` 要放在其它 media query 之后**，或给动画加 `screen` 限定。否则滚动动画的
   `opacity:.25` 起始态会被打印出来，整页半透明。
10. **超过 8 天的预报不可靠**。实测 ECMWF 与 GFS 对 11 天后同一点的最高温分歧达 9.7°C
    （5 天内分歧仅 0.1°C）。这是大气可预报性上限，不是数据源问题。>8 天要显式标注参考性低，
    并给出历史同期作为对照。
11. **海拔是地形模型格点值**（约 1km 分辨率），与实际路面可能差几百米（折多山标 4774m，
    垭口路面约 4300m）。当量级参考，别当精确值。
12. **分享页不能有任何外部请求**，且独立版必须自带 `<meta charset="utf-8">`，否则单独打开
    中文乱码。托管版则不能有 `<head>`（平台会补）。
13. **小红书图链带时效签名**，过期即失效。所以分享页只内嵌高德图，小红书只留标题 + 原帖链接
    （链接必须带 `xsec_token`，否则打开是「当前内容无法查看」）。

## 小红书（可选）

要用就得跟用户要 Cookie：网页登录 xiaohongshu.com → F12 → Network → 任意请求的
Request Headers → 复制整条 `Cookie`。写入项目目录 `.xhs_cookie`（**`chmod 600` 并加进
`.gitignore`；这是登录态，泄漏等于账号被接管**）。

签名引擎移植自 TripStar（GPL-2.0），底层是 Spider_XHS 的 JS 签名。**用了它，产出物受 GPL 约束**（详见 `NOTICE.md`，不需要就删掉 `tools/xhs_sign/` 和 `tools/xhs_photos.py`，其余功能不受影响）。
cookie 过期表现为 `code=-100 登录已过期`，重新取即可。

## 部署到自己的服务器

只需要 `index.html` + `data.json` 两个文件。**绝不能传** `.xhs_cookie`、`.cache/`、
`*.py`、`xhs_sign/`、`node_modules/`。建议打个 `dist/` 再传，并 grep 确认没有
`web_session` / Web服务 key 混进去。

nginx 要点：`location` 用 `^~` 避免被 SPA fallback 吃掉；`gzip on`（data.json 通常能压到
14%）；高德安全密钥用官方代理方式（`location /_AMapService/` 注入 `jscode`），前端配
`serviceHost` 而非明文密钥。`index.html` 顶部按 `location.hostname` 自动切换本地/线上
两套高德配置，所以一份文件本地和线上通用，部署时不用改。

JS API key 配了域名白名单后 localhost 会报 `INVALID_USER_DOMAIN` —— 本地和线上用两个 key。

## 输出物

| 文件 | 用途 |
|---|---|
| `data.json` | 结构化行程数据，所有页面的数据源 |
| `index.html` | 本地交互版：高德底图、日期切换、点开看大图与原帖链接 |
| `<标题>.html` | 独立单文件分享版，离线可看 |
| `share.html` | 托管版正文 |
| `.cache/` | 接口缓存（限流保护），可安全删除，删了只是下次重抓 |
