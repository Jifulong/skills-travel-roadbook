# ---- 绝不能提交：凭据 ----
# 注意: .gitignore 不支持行尾注释, 注释必须独占一行,
# 否则整行(含 # 后面的字)会被当成模式串, 规则静默失效。
# 小红书登录态, 泄漏等于账号被接管
.xhs_cookie
*.cookie
.env
secrets.json

# ---- 接口缓存与产出物（体积大且可重新生成）----
.cache/
data.json
share.html
dist/
*.pdf
路书图片版/
# 独立分享版(体积 5MB 上下, 每次重新生成)
*路书.html
*roadbook*.html

# ---- 依赖 ----
node_modules/
package-lock.json
pnpm-lock.yaml
.venv/
venv/
__pycache__/
*.py[cod]

# ---- 每条线路自己的输入(示例在 examples/, 实际使用时在项目目录里)----
/itinerary.txt
/aliases.json

# ---- 系统 ----
.DS_Store
Thumbs.db
*.swp
